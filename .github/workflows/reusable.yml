name: reusable workflow
on:
  workflow_call:
    inputs:
      name:
        description: to enter the name
        required: true
        type: string
jobs:
  reusable-job:
    runs-on: ubuntu-latest
    steps:
      - name: greet
        run: echo " hello ${{inputs.name}} good evening"
