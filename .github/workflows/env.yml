name: env demo
on: 
 workflow_dispatch:
   inputs:
     environment:
       description: choose the env to run the workflow
       required: true
       default: dev
jobs:
  env-job:
    runs-on: ubuntu-latest
    steps:
      - name: print name of the environment
        run: echo " the env is ${{inputs.environment}}"
