name: build and deploy
on:
 pull-request:
   types:
     - opened
     - synchronze
jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run : echo "building the job"
